[repository]
name = "hypr-agent-protal"
authors = ["wilf"]
commit_pins = [
    ["0.54.3", ""],
    ["0.55.0", ""]
]

["hypr-agent-protal"]
description = "Background agent primitives for Hyprland"
authors = ["wilf"]
output = "build-hyprpm/libhypr-agent-protal.so"
build = [
    "cmake -S . -B build-hyprpm -DCMAKE_BUILD_TYPE=Release",
    "cmake --build build-hyprpm",
    "ctest --test-dir build-hyprpm --output-on-failure"
]
